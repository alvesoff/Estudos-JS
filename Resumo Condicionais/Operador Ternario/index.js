const velocidade = 90;
const warn = 50;

// condição ? true : false

const condicao =
 velocidade >= warn 
 ? console.log("Velocidade alta!") 
 : console.log("Velocidade normal.");


 // Isso equivale ao IF e ELSE abaixo:
/*
if (velocidade >= warn) { console.log("Velocidade alta!"); } 
else { console.log("Velocidade normal."); }
*/